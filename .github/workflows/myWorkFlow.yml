name: My Workflow
on:
  push:
    branches: [main]
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
    - name: My Custom Action
      uses: ./my-custom-action
      with:
        message: "Hello world"
